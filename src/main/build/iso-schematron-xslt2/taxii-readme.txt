Use "ExtractSchFromXSD-2.xsl" to extract Schematron from the XSD schema files.

Use the "iso_schematron_message_xslt2.xsl" stylesheet to compile the Schematron file extracted from the schema
into a stylesheet.